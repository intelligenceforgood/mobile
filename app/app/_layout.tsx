import { Stack } from 'expo-router';

/**
 * Root layout for the I4G mobile app.
 * Sprint 0: Stack navigator only — no providers yet.
 * Providers (QueryClientProvider, AuthProvider, etc.) will be added in Sprint 1.
 */
export default function RootLayout() {
  return <Stack />;
}
