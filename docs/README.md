# Mobile Docs

This mirrors the planning artifacts in `planning/mobile/` for convenience once this repo is split/published.

- PRD/TDD/Roadmap: see `planning/mobile/` (or copy here when repo is standalone).
- Design tokens: `shared/design-tokens/`.
- API schemas: `shared/api-schemas/`.
