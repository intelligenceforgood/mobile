# Mobile Docs

This mirrors the planning artifacts in `proto/planning/mobile/` for convenience once this repo is split/published.

- PRD/TDD/Roadmap: see `proto/planning/mobile/` (or copy here when repo is standalone).
- Design tokens: `shared/design-tokens/`.
- API schemas: `shared/api-schemas/`.
